class Constant {
    // Login Page
    static emaiRequiredlError = "Email is required"
    static pwRequiredlError = "Password is required"
    static invalidEmailError = "Enter a valid email"
    static notEnoughLengtPwError = "Password should be of minimum 8 characters length"
    static messAlert = "Your login attempt has failed. Make sure the username and password are correct."

    // HomePage
        // Left Menu
        static EngLeftMenuList = ["Dashboard", "Workspaces", "Logs"]
        static JapLeftMenuList = ["ダッシュボード", "ワークスペース", "ログ"]
}

module.exports = { Constant }